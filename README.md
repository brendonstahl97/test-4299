# PB&JJ Site

This is the Repository for the PB&JJ Podcast website.

Currently, this is built with Vite and React and uses both AnimeJS and Motion for animation with the intent to be hosted on Github Pages

## Running the Project

To run the project locally: 
1. clone the repository
2. Run `npm i`
3. Run the dev script with `npm run dev`